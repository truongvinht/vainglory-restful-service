// baseModel.js
// base model class sharing common methods
// ==================

'user strict';

/**
 * Base model for sharing helper methods
 * @class 
 * @module BaseModel
 */
class BaseModel {

    constructor(){

    }

    /**
     * Convert parameters to a String and separate by comma (,)
     * @param {array} list entries as array
     */
    listToString(list) {
        return "";
    }
};

module.exports = BaseModel;